import { TodoPannel } from "../components/todoPannel";

export const List = () => {
    return (
        <div className="list_container">
            <TodoPannel status="list" />
        </div>
    );
};
